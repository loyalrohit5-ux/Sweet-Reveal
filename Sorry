import { useState } from "react";

export default function ApologyApp() {
  const messages = [
    "I messed up… and I’m really sorry for that.",
    "I promise I’ll be better for you.",
    "Please forgive me… You mean so much to me.",
    "I know I hurt you… and I’ve been feeling bad about it."
  ];

  const [index, setIndex] = useState(0);
  const [show, setShow] = useState(false);

  return (
    <div className="min-h-screen flex items-center justify-center bg-gradient-to-br from-pink-100 to-purple-100 p-4">
      <div className="bg-white/80 backdrop-blur-xl rounded-3xl shadow-xl p-6 w-full max-w-sm text-center">
        {!show ? (
          <>
            <h1 className="text-xl font-semibold text-gray-700 mb-4">Hey Cutiepie 💗</h1>
            <p className="text-gray-500 mb-6">Can we talk for a moment? There's something important I want to tell you.</p>
            <button
              onClick={() => setShow(true)}
              className="px-6 py-2 rounded-full bg-pink-400 text-white shadow-md hover:bg-pink-500 transition"
            >
              Continue 💖
            </button>
          </>
        ) : (
          <>
            <h2 className="text-lg font-medium text-gray-700 mb-4">From my heart…</h2>
            <div className="rounded-2xl bg-gradient-to-r from-pink-200 to-purple-200 p-6 text-gray-700 min-h-[120px] flex items-center justify-center">
              {messages[index]}
            </div>
            <button
              onClick={() => setIndex((index + 1) % messages.length)}
              className="mt-6 px-6 py-2 rounded-full bg-purple-400 text-white shadow-md hover:bg-purple-500 transition"
            >
              Next ➜
            </button>
          </>
        )}
      </div>
    </div>
  );
}
